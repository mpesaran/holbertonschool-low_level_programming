#include "main.h"

/**
 * mul - multiplies two integer
 * @a: first number to multiply
 * @b: second nuber to multiply
 *
 * Return: multiply of integers given
 */
int mul(int a, int b)
{
	int prod = a * b;

	return (prod);
}
